[tool.ruff]
# Target Python version (Python 3.12 is used in Dockerfiles)
target-version = "py312"

# Standard line length limit matching Black/Ruff defaults
line-length = 88

# Source folders containing Python files
src = ["autoscaler", "monitor", "scripts"]

# Exclude build, cache, generated showcase repos, and data directories
exclude = [
    ".git",
    "__pycache__",
    "build",
    "dist",
    "n8n-data",
    "backups",
    "published_repos",
    "mcp_server",
]

[tool.ruff.lint]
# Enable Pyflakes (F), pycodestyle (E, W), isort (I), Upgrade (UP), Ruff-specific (RUF) rules
select = [
    "F",   # Pyflakes
    "E",   # pycodestyle errors
    "W",   # pycodestyle warnings
    "I",   # isort
    "UP",  # pyupgrade
    "RUF"  # Ruff-specific rules
]
# Ignore line-too-long (E501) as code is formatted via ruff format (per Ruff official guidelines)
ignore = ["E501"]

[tool.ruff.lint.isort]
combine-as-imports = true

